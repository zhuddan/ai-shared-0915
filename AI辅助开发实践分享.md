# AI 辅助开发实践分享

> 非 PRD 或 Plan 模式下的 vibe coding

AI 辅助开发的六个关键环节：**规范输入、约束生成、明确验收、沉淀 Skill、主动学习、定期复盘**。

## 1. 规范输入：把提示词写清楚

**开发者必须比 AI 更清楚要做什么**

- 目标文件/接口地址
- 项目内的专业术语
- 具体功能和交互行为
- 已确定的技术方案
- 允许和禁止的修改范围
- 验收标准和检查命令

错误示例：

> 参考项目里的测点配置页面，帮我新增一个软测量页面。左侧展示树形结构，点击不同节点后，右侧显示对应的配置组件。页面布局和交互尽量与现有页面保持一致。

优化示例：

> 修改 `@xxx.tsx`，使用 `/create-tree-page` Skill 完成软测量页面：
>
> - 左侧复用项目现有的 `Tree` 组件，右侧作为内容展示区。
> - 点击树节点后，右侧展示与当前节点对应的组件，并保持选中状态一致。
> - 使用 `Context` 减少 props 透传，确保 Context value 的引用稳定，避免无意义重渲染。
> - 不要修改现有接口、路由及无关文件。
> - 完成后运行类型检查、代码风格检查及相关测试，并报告执行结果。

处理 Bug 时应说明：

- 当前行为
- 预期行为
- 复现步骤
- 错误信息或数据示例

错误示例：

> 这个刷新问题还没解决。保存后列表已经更新了，但点开详情看到的还是旧数据，重新进入页面才会正常，帮我再看一下。

优化示例：

> 修复保存后详情数据未刷新的问题：
>
> - 当前行为：保存成功后列表已刷新，但详情接口没有重新请求，详情区域仍显示旧数据。
> - 预期行为：保存成功后重新请求详情接口，并展示最新数据。
> - 复现步骤：进入编辑页修改数据并保存，保存成功后查看当前详情区域。
> - 排查重点：检查详情查询的 `queryKey`、调用参数与 `invalidateQueries` 使用的 key 是否一致。
> - 验收要求：保存后列表和详情均展示最新数据，且不产生重复请求。

## 2. 约束生成：AI 代码也必须遵守规范

**AI 生成也要守规范**

- 类型：ypeScript 禁止使用 `any`
- 注释：注释重点解释“为什么”，不要复述简单代码
- 结构：合理拆分
- 范围：只修改与当前需求直接相关的代码

## 3. 明确验收：结果必须可以验证

**用验收对抗概率**

- 代码风格检查：ESLint / Oxlint
- 类型检查：`tsc --noEmit` 或项目提供的类型检查命令
- 测试：单元测试 / 关键流程的 E2E 测试
- 人工 Review

## 4. 沉淀 Skill：职责单一，边界清晰

**把经验变成可复用的 Skill**

- `/skill-creator`
- 清晰的边界：明确何时触发、何时不触发，避免与其他 Skill 职责重叠
- 完整的步骤：写清输入、输出、限制和验证方式
- 真实的模板：按需提供项目中的代码、目录和示例
- 稳定的表达：保持结构和术语统一；

## 5. 主动学习：看不懂 AI 代码就继续追问

**AI 可以加速理解，但不能代替理解**

- 请按照数据流解释这段代码：数据从哪里来，经过什么转换，最后在哪里使用？
- 为什么这里需要 `useCallback`？不使用会发生什么？为什么选择这个方案？
- 这段代码能否简化？请比较两种方案的维护成本。

## 6. 定期复盘：把经验变成下一次的规则

**让复盘变成规则**

- Claude Code 提供了 `/insights`，可以分析一段时间内的使用记录。拿到分析结果后，还可以继续让 Claude 总结问题并优化工作方式。
- Codex 没有对应的 `/insights` 命令，但可以通过分析本地使用记录实现类似的复盘。

## 总结

AI 是概率模型，不能保证每次都正确。高质量使用 AI 的关键是建立完整闭环：

1. 输入清晰：目标、上下文和边界明确。
2. 生成受控：代码符合项目规范。
3. 结果可验：验收标准具体且可执行。
4. 能力沉淀：Skill 职责单一、边界清晰。
5. 保持理解：开发者能够解释并维护代码。
6. 持续复盘：将有效经验沉淀为规则、Skill 或脚本。



## AI 复盘提示词：

```
分析 ~/.codex/history.jsonl 和 ~/.codex/sessions/ 目录下最近 30 天的 Codex 使用记录。

不要只做 token 统计，重点分析我的使用行为：

1. 我最常让 Codex 做哪些类型的任务
2. 哪些任务我经常反复修改或追问
3. 哪些提示词写法导致 Codex 经常理解错误
4. 哪些任务适合一次性提供更多上下文
5. 哪些任务适合拆分
6. 我有哪些重复操作可以用 AGENTS.md、Skill、脚本或自定义命令解决
7. 我在哪些场景浪费了较多 token
8. 总结我使用 Codex 的习惯和问题
9. 给出 10 条最值得实施的改进建议

最后生成：

- 使用画像
- 高频任务分类
- 低效模式
- Prompt 改进建议
- AGENTS.md 改进建议
- 自动化建议

不要修改任何文件。
```
