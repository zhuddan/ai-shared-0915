---
theme: nord
title: AI 使用技巧分享
info: 聊聊怎么捣鼓 AI 写代码
transition: slide-left
layout: center
class: text-center
---


# AI 辅助开发实践分享

<p>聊聊怎么捣鼓 AI 写代码</p>

---

## AI 六个关键环节：

<br>

1. 规范输入
2. 约束生成
3. 明确验收
4. 沉淀 Skill
5. 主动学习
6. 定期复盘

---

# 1. 规范输入：把提示词写清楚

**开发者必须比 AI 更清楚要做什么**

- 目标文件 / 接口地址 / 其他确定内容
- 专业术语
- 具体功能 / 交互行为
- 已确定的技术方案
- 允许和禁止的修改范围 
- 验收标准和检查命令

---

## 普通需求

❌错误示例：

```md
参考项目里的测点配置页面，帮我新增一个软测量页面。
左侧展示树形结构，点击不同节点后，右侧显示对应的配置组件。页面布局和交互尽量与现有页面保持一致。
```

✅优化示例：

```md
修改 `@xxx.tsx`，使用 `/create-tree-page` Skill 完成软测量页面：

- 左侧复用项目现有的 `Tree` 组件，右侧作为内容展示区。
- 点击树节点后，右侧展示与当前节点对应的组件，并保持选中状态一致。
- 使用 `Context` 减少 props 透传，确保 Context value 的引用稳定，避免无意义重渲染。
- 不要修改现有接口、路由及无关文件。
- 完成后运行类型检查、代码风格检查及相关测试，并报告执行结果。
```
---

## 处理 Bug 

<br />

- 当前行为
- 预期行为
- 复现步骤
- 错误信息或数据示例

❌错误示例：

```md
这个刷新问题还没解决。保存后列表已经更新了，但点开详情看到的还是旧数据，重新进入页面才会正常，帮我再看一下。
```

✅优化示例：

```md
修复保存后详情数据未刷新的问题：

- 当前行为：保存成功后列表已刷新，但详情接口没有重新请求，详情区域仍显示旧数据。
- 预期行为：保存成功后重新请求详情接口，并展示最新数据。
- 复现步骤：进入编辑页修改数据并保存，保存成功后查看当前详情区域。
- 排查重点：检查详情查询的 `queryKey`、调用参数与 `invalidateQueries` 使用的 key 是否一致。
- 验收要求：保存后列表和详情均展示最新数据，且不产生重复请求。
```


---

# 2. 约束生成：AI 代码也必须遵守规范

**AI 生成也要守规范**

- 命名：文件命名 / 代码命令
- 编码：`TypeScript` 禁止使用 `any`
- 注释：注释重点解释“为什么”，不要复述简单代码
- 结构：合理拆分
- 范围：只修改与当前需求直接相关的代码
- 其他：其他规范

[规范参考](vscode://file/Users/zd/Workspace/yth/yth-mpc-frontend/AGENTS.md:31:0)

---

# 3. 明确验收：结果必须可以验证

**用验收对抗概率**

- 风格检查：`ESLint` / `Oxlint` / `Checkstyle`
- 类型检查：`tsc --noEmit` 或项目提供的类型检查命令
- 集成测试：单元测试 / 关键流程的 `E2E` 测试
- 编译打包：`pnpm build`

- 🚩**人工 Review** 

[校验参考](vscode://file/Users/zd/Workspace/yth/yth-mpc-frontend/AGENTS.md:14:0)

---

# 4. 沉淀 Skill：职责单一，边界清晰

**把经验变成可复用的 Skill**

- 创建 SKILL : `/skill-creator`
- 清晰的边界：明确何时触发、何时不触发。**建议主动触发**
- 完整的步骤：写清输入、输出、限制和验证方式
- 真实的模板：**按需** 提供项目中的代码、目录和示例
- 稳定的表达：保持结构和术语统一

[yth-api/SKILL.md](vscode://file/Users/zd/.agents/skills/yth-api/SKILL.md)

[yth-api/assets/service-template.ts](vscode://file/Users/zd/.agents/skills/yth-api/assets/service-template.ts)

---

# 5. 主动学习：看不懂 AI 代码就继续追问

**AI 可以加速理解，但不能代替理解**

```ts
/**
 * 接口 Url 各字段是否存在的位标记(bitmask)
 * 用于快速判断当前解析结果属于哪一种组合场景,避免写多个 && 判断
 */
export const UrlPartFlag = {
  /** 端口存在,对应二进制第 1 位 */
  PORT: 1 << 0, // 0b001 = 1
  /** IP 存在,对应二进制第 2 位 */
  IP: 1 << 1, // 0b010 = 2
  /** 协议存在,对应二进制第 3 位 */
  PROTOCOL: 1 << 2, // 0b100 = 4
} as const;
```

---

# 6. 定期复盘：把经验变成下一次的规则

**复盘变成规则**

- `Claude Code` 提供了 `/insights`，可以分析一段时间内的使用记录。拿到分析结果后，还可以继续让 `Claude` 总结问题并优化工作方式。
- 其他`AI Agent` 如 `Codex` 虽然没有对应的 `/insights` 命令，但可以通过分析本地使用记录实现类似的复盘。

---

# 小结

AI 是概率模型，不能保证每次都正确。

- 输入清晰：目标、上下文和边界明确。
- 生成受控：代码符合项目规范。
- 结果可验：验收标准具体且可执行。
- 能力沉淀：Skill 职责单一、边界清晰。
- 保持理解：开发者能够解释并维护代码。
- 持续复盘：将有效经验沉淀为规则、Skill 或脚本。

---

# 补充

- 英文化提示词 (国外ai)
- 范围指定
- 非主动触发 `skill`
- 具体任务
- `Markdown`

---


# 附：AI 复盘提示词

```md
分析 ~/.codex/history.jsonl 和 ~/.codex/sessions/ 目录下最近 30 天的 Codex 使用记录。

不要只做 token 统计，重点分析我的使用行为：

1. 我最常让 Codex 做哪些类型的任务
2. 哪些任务我经常反复修改或追问
3. 哪些提示词写法导致 Codex 经常理解错误
4. 哪些任务适合一次性提供更多上下文
5. 哪些任务适合拆分
6. 我有哪些重复操作可以用 AGENTS.md、Skill、脚本或自定义命令解决
7. 我在哪些场景浪费了较多 token
8. 总结我使用 Codex 的习惯和问题
9. 给出 10 条最值得实施的改进建议

最后生成：

- 使用画像
- 高频任务分类
- 低效模式
- Prompt 改进建议
- AGENTS.md 改进建议
- 自动化建议
```
---
layout: center
---

# 感谢观看
